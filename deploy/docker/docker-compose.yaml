version: '3.8'

services:
  vigil-frontend:
    image: vigil-frontend:latest
    build:
      context: ./frontend
    ports:
      - "3301:3301"
    environment:
      - REACT_APP_API_URL=http://localhost:8080
    depends_on:
      - vigil-query-service

  vigil-query-service:
    image: vigil-query-service:latest
    build:
      context: ./pkg/query-service
    ports:
      - "8080:8080"
    environment:
      - CLICKHOUSE_HOST=clickhouse
      - CLICKHOUSE_PORT=9000
    depends_on:
      - clickhouse

  clickhouse:
    image: clickhouse/clickhouse-server:24.3
    ports:
      - "9000:9000"
      - "8123:8123"
    volumes:
      - clickhouse_data:/var/lib/clickhouse

volumes:
  clickhouse_data:

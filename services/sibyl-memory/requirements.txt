# VIGIL MEMORY — Sibyl memory service (PostgreSQL backend).
#
# Rewritten to use Supabase PostgreSQL instead of SQLite/sibyl_memory_client.
# No disk required. No new accounts. Uses the existing Supabase DB.
psycopg2-binary==2.9.10

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

openapi: 3.0.0
info:
  title: VIGIL Control Plane API
  version: 1.0.0
  description: API for the VIGIL Autonomous Reliability Guardian for AI Agents.
servers:
  - url: http://localhost:8080/api/v1
paths:
  /health:
    get:
      summary: Health Check
      responses:
        '200':
          description: OK
  /vigil/agents:
    get:
      summary: List all live agents
      responses:
        '200':
          description: A list of agents
  /vigil/agents/{id}/kill:
    post:
      summary: Kill an active agent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Agent terminated
  /vigil/agents/{id}/pause:
    post:
      summary: Pause an active agent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Agent paused
  /vigil/agents/{id}/resume:
    post:
      summary: Resume a paused agent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Agent resumed
